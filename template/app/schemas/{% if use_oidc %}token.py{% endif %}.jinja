from pydantic import BaseModel


class TokenPayload(BaseModel):
    # The standard OIDC unique user ID
    sub: str

    # Our internal helper to pass the JWT to the UserInfo endpoint
    raw_token: str
